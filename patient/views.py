from django.shortcuts import render
from django.http import HttpResponse
import json
from patient.models import "model name"
# Create your views here.

#returns patient id
def getPtId():
    if request.method == 'GET':
            response = json.dumps(#user first name)

    return HttpResponse(response,content_type= 'text/json')

#returns Patient First Name
def getPtFName(request):
    if request.method == 'GET':
            response = json.dumps(#user first name)

    return HttpResponse(response,content_type= 'text/json')

#returns Patient Last Name
def getPtLName(request):
    if request.method == 'GET':
            response = json.dumps(#user first name)

    return HttpResponse(response,content_type= 'text/json')

#returns Patient Full name
def getPtFullName(request):
    if request.method == 'GET':
            response = json.dumps(#user first name)

    return HttpResponse(response,content_type= 'text/json')

#returns Patient DOB
def getPtDOB(request):
    if request.method == 'GET':
        response = json.dumps(  # user first name)

    return HttpResponse(response, content_type='text/json')

#returns street number
def getPtAddr(request):
    if request.method == 'GET':
        response = json.dumps(  # user first name)

    return HttpResponse(response, content_type='text/json')

#returns city
def getPtCity(request):
    if request.method == 'GET':
        response = json.dumps(  # user first name)

    return HttpResponse(response, content_type='text/json')

#returns state
def getPtState(request):
    if request.method == 'GET':
        response = json.dumps(  # user first name)

    return HttpResponse(response, content_type='text/json')

#returns zip
def getPtZip(request):
    if request.method == 'GET':
        response = json.dumps(  # user first name)

    return HttpResponse(response, content_type='text/json')

#returns full address
def getPtAddress(request):
    if request.method == 'GET':
        response = json.dumps(  # user first name)

    return HttpResponse(response, content_type='text/json')

